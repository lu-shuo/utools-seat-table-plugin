# 快速开始指南

## 🚀 首次设置

### 1. 安装项目依赖
```bash
# 安装主项目依赖
npm install

# 安装 preload 依赖（重要！）
cd public/preload
npm install
cd ../..
```

### 2. 开发模式
```bash
npm run dev
```
然后在 uTools 中输入插件关键词（座位、排座位等）即可打开。

### 3. 生产构建
```bash
npm run build:plugin
```

### 4. 验证构建
```bash
npm run verify
```

## 📦 安装到 uTools

### 开发模式安装
1. 打开 uTools
2. 输入「插件应用」
3. 点击「开发者」→「添加」
4. 选择项目根目录下的 `plugin/` 文件夹

### 验证安装成功
1. 在 uTools 中输入「座位」触发插件
2. 按 F12 打开开发者工具
3. 在 Console 中输入 `window.services`
4. 应该看到一个包含 `readFile`, `writeTextFile`, `readExcelFile` 等方法的对象

## 🔧 常用命令

| 命令 | 说明 |
|------|------|
| `npm run dev` | 启动开发服务器 |
| `npm run build` | 构建前端资源 |
| `npm run build:plugin` | 完整构建插件（推荐） |
| `npm run verify` | 验证插件构建 |

## ⚠️ 常见问题

### window.services 为 undefined
**原因：** preload 依赖未安装

**解决：**
```bash
cd public/preload
npm install
cd ../..
npm run build:plugin
```

### 开发模式正常，打包后不工作
**原因：** 忘记在 preload 目录安装依赖

**解决：** 参考上面的解决方案

### 打包文件过大
**当前大小：** 约 11-13 MB（主要是 xlsx 库）

**优化方案：**
- 考虑使用更轻量的 Excel 库
- 或者只打包 xlsx 的核心功能

## 📝 开发流程

### 添加新的 Node.js 功能
1. 在 `public/preload/services.js` 中添加方法
2. 在 Vue 组件中通过 `window.services.yourMethod()` 调用

### 添加新的前端功能
1. 在 `src/` 目录下创建组件
2. 在 `src/App.vue` 中注册路由
3. 在 `public/plugin.json` 中添加功能定义

## 🎯 发布流程

1. 测试所有功能
```bash
npm run dev
# 测试各项功能
```

2. 构建插件
```bash
npm run build:plugin
```

3. 验证构建
```bash
npm run verify
```

4. 安装测试
- 在 uTools 中安装 plugin 目录
- 测试所有功能是否正常

5. 打包发布
```bash
# 压缩 plugin 目录为 zip
# 将 .zip 改名为 .upx
```

## 💡 提示

- 开发时使用 `npm run dev`，可以热更新
- 打包前务必运行 `npm run verify` 检查
- preload 脚本的日志会输出到 uTools 开发者工具的 Console
- 如果修改了 preload 脚本，需要重启 uTools 才能生效
